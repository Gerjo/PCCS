#include "VCircle.h"

namespace vor{
    namespace geom{
        VCircle::VCircle(Vector3 position, float radius){
            this->position = position;
            this->radius = radius;
        }
    }
}